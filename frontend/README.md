# 健康智护 - 前端桌面应用开发文档

## 项目概述

健康智护（HealthSmart）前端采用JavaFX + WebView混合架构，使用HTML/CSS/JavaScript构建Web界面，在JavaFX容器中运行，提供Windows桌面应用体验。

## 技术栈

- **JavaFX 17** - Windows桌面应用框架
- **WebView** - 在JavaFX中嵌入Web内容
- **HTML5/CSS3** - 界面结构与样式
- **JavaScript (ES6+)** - 前端交互逻辑
- **Gson** - JSON数据处理

## 项目结构

```
frontend/
├── java_core/                      # JavaFX桌面应用模块
│   ├── pom.xml                    # Maven配置文件
│   └── src/main/java/com/healthsmart/
│       └── frontend/
│           ├── HealthSmartApp.java # 主应用程序入口
│           ├── controller/         # API控制器层
│           │   ├── UserController.java
│           │   ├── HealthReportController.java
│           │   ├── PlanController.java
│           │   └── AnalyticsController.java
│           └── server/            # REST API服务器
│               └── ApiServer.java
└── web/                            # Web界面资源
    ├── index.html                 # 主页面
    ├── css/
    │   └── app.css               # 应用样式
    ├── images/                   # 图片资源（空目录）
    │   └── logo.png
    └── js/
        └── api.js                # API通信模块
```

## 功能模块

### 1. 用户认证模块 (UserController)
- 用户登录界面交互
- 用户注册界面交互
- 会话管理
- 用户信息展示

### 2. 健康报告模块 (HealthReportController)
- 健康报告表单展示
- 数据提交与验证
- 历史报告查看

### 3. 计划展示模块 (PlanController)
- 今日计划展示
- 饮食建议卡片
- 运动计划卡片
- 任务完成状态管理

### 4. 数据分析模块 (AnalyticsController)
- 完成度统计展示
- 连续打卡天数
- 趋势图表数据

## 快速开始

### 环境要求
- Java 17+
- Maven 3.6+

### 运行前端应用

**方式一：使用Maven运行**
```bash
cd frontend/java_core
mvn javafx:run
```

**方式二：打包后运行**
```bash
cd frontend/java_core
mvn package
java -jar target/healthsmart-frontend-1.0.0.jar
```

## 与后端通信

### API配置
- **默认地址**: `http://localhost:8080/api`
- **通信协议**: HTTP REST API
- **数据格式**: JSON

### API端点

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/user` | GET/POST | 用户认证 |
| `/api/health-report` | POST | 健康报告 |
| `/api/plan` | GET/POST | 计划管理 |
| `/api/analytics` | GET/POST | 数据分析 |
| `/api/health` | GET | 健康检查 |

### 前端API调用示例 (api.js)

```javascript
const API_BASE = 'http://localhost:8080/api';

// 用户登录
async function login(username, password) {
    const response = await fetch(`${API_BASE}/user?action=login`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ username, password })
    });
    return await response.json();
}

// 获取今日计划
async function getTodayPlan(userId) {
    const response = await fetch(`${API_BASE}/plan?action=getToday`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ userId })
    });
    return await response.json();
}
```

## 界面结构

### 主页面 (index.html)
```
┌─────────────────────────────────────────────────────────┐
│  [健康智护 LOGO]           用户: 张三    [登出]        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│  │  今日   │ │  健康   │ │  计划   │ │  数据   │      │
│  │  任务   │ │  报告   │ │  列表   │ │  统计   │      │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │                    今日计划                      │   │
│  │  ┌─────────┐  饮食建议  ┌─────────────────┐     │   │
│  │  │ 早餐    │ ───────── │ 早餐: 燕麦粥     │     │   │
│  │  │ 🍳     │           │ 鸡蛋: 2个        │     │   │
│  │  │ ⬜     │ ───────── │ 牛奶: 250ml      │     │   │
│  │  └─────────┘           └─────────────────┘     │   │
│  │                                                 │   │
│  │  ┌─────────┐  运动计划  ┌─────────────────┐     │   │
│  │  │ 运动    │ ───────── │ 快走: 30分钟     │     │   │
│  │  │ 🏃     │           │ 消耗: ~200kcal   │     │   │
│  │  │ ⬜     │ ───────── │ 注意事项: 充分    │     │   │
│  │  └─────────┘           └─────────────────┘     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 相关文档

- [项目总览](../README.md) - 项目主文档
- [后端开发文档](../backend/README.md) - 后端技术文档
- [前端需求规格](../.cospec/frontend-display/requirements.md) - 前端功能需求
- [需求规格](../深信服比赛项目简介.txt) - 原始需求文档

## 许可证

本项目仅供学习和研究使用。